# Paths
DATA_DIR=~/.analyzer/data
DB_PATH=~/.analyzer/data/analyzer.db
CHROMA_PATH=~/.analyzer/data/chroma

# Ollama settings
OLLAMA_HOST=http://localhost:11434
OLLAMA_LLM_MODEL=qwen2.5:7b-instruct
OLLAMA_EMBEDDING_MODEL=nomic-embed-text

# LLM settings
LLM_TEMPERATURE=0.1
LLM_MAX_TOKENS=4096

# Extraction settings
REQUIREMENT_KEYWORDS=["must", "shall", "should", "required", "mandatory", "obligatory"]

# Chunk settings
MAX_CHUNK_SIZE=2000
CHUNK_OVERLAP=200

# Retrieval settings
DEFAULT_TOP_K=5
SIMILARITY_THRESHOLD=0.7

# Playbook settings
MIN_SECTION_FREQUENCY=0.3
REQUIRED_SECTION_THRESHOLD=0.8
